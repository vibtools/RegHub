# v0.3.1.2 Master Validation Gates

Promotion is blocked unless all of the following pass:

1. `python -m pip check`
2. Ruff lint and formatting
3. Python compilation
4. exactly one Alembic head
5. PostgreSQL migration upgrade and seed
6. full pytest suite with at least 70% application coverage
7. exact third-party dependency snapshot excluding only `reghub`
8. strict `pip-audit` without dependency re-resolution
9. Docker image build
10. container readiness check against `/api/v1/ready`

Main must not be updated before both GitHub Actions jobs are green.
