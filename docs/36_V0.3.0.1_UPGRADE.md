# RegHub v0.3.0.1 Upgrade

1. Back up PostgreSQL and record the healthy v0.3.0 deployment.
2. Replace source files while preserving `.git`, `.env`, Coolify variables, Keycloak, and PostgreSQL.
3. Commit, push, and redeploy the web application. No Alembic migration is added.
4. Verify `/admin/governance`, `/admin/settings`, `/api/v1/ready`, and `/api/v1/capabilities`.
5. Keep **Redis Operation Worker** OFF unless Redis and the separate worker service are already healthy.
6. To prepare future scale, provision Redis and a worker with `OPERATION_BACKEND=redis`; confirm the
   worker heartbeat on Governance, then turn the feature ON from Settings.

Rollback to the previous v0.3.0 commit if required. The added runtime feature row is harmless to v0.3.0.
