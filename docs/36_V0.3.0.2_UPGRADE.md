# RegHub v0.3.0.2 Upgrade

1. Do not redeploy v0.3.0.1 while its CI run is red. The failure occurred in the Ruff gate before the remaining tests and Docker checks.
2. Replace the repository files with the v0.3.0.2 release. Preserve `.git`, `.env`, Coolify variables, PostgreSQL, Keycloak, and domains.
3. Commit and push.
4. Wait for GitHub Actions to become green.
5. Redeploy in Coolify only after the full workflow passes.

No database migration is included.
