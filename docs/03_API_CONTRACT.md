# API Contract

The public contract is rooted at `/api/v1`. Only published templates are visible.
List endpoints are deterministic, bounded, and read-only. Manifest schema `1.0` is strict.
Breaking response changes require `/api/v2`.
