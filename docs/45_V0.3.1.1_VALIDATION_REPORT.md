# RegHub v0.3.1.1 Validation Report

## Scope compliance

The patch is limited to the unfinished v0.3.1.0 validation items:

- tracked generated artifacts;
- stale duplicate build source;
- historical analysis normalization;
- dead placeholder files;
- migration and regression evidence.

## Repository validation

Expected post-patch state:

- `build/` absent;
- `dist/` absent;
- only `app/main.py` exists as a runtime application entrypoint;
- no `BASELINE_COMPATIBILITY_V*.txt`;
- no `FILE_INVENTORY_V*.txt`;
- no tracked `RELEASE_VERIFICATION_V*.txt`;
- no Cloudflare, Coolify or Docker placeholder integration README files.

## Migration validation

Migration `20260722_0008`:

- updates data only;
- creates or drops no table;
- adds or drops no column;
- normalizes templates, template versions and import history;
- preserves registry metadata;
- uses deterministic values and `IS DISTINCT FROM`;
- is safe to execute repeatedly at the SQL-operation level;
- runs through the existing automatic Alembic entrypoint.

## Regression lock

No application service, route, schema model, administrator view, integration client, analyzer module,
Dockerfile, CI workflow or entrypoint is modified beyond release-version reporting.

## Promotion decision

Promote only after the unchanged GitHub Actions workflow passes on the applied repository commit.
