# RegHub v0.3.1.1 Final Stabilization Hotfix

## Baseline

This hotfix applies only to RegHub v0.3.1.0 at commit:

```text
4f562f6e9c463c22a414eabcf376265c5d0c4484
```

## Purpose

The v0.3.1.0 validation found that the active runtime stabilization was correct but repository and
historical-data cleanup remained incomplete. v0.3.1.1 finishes those exact items without changing
architecture or product behavior.

## Changes

1. Removes tracked build/distribution output and generated root reports.
2. Removes the stale duplicate `build/lib/app` application.
3. Removes unused Cloudflare, Coolify and Docker placeholder integration files.
4. Adds data-only migration `20260722_0008`.
5. Normalizes current and historical analysis JSON to the existing registry-only structure.
6. Adds repository-hygiene, migration-scope, version and deployment-contract regression tests.
7. Updates release identity and release documentation to `0.3.1.1`.

## Explicit non-changes

- No endpoint or response model change.
- No table or column change.
- No active analyzer behavior change.
- No dependency change.
- No administrator page or Setting change.
- No Docker, CI, entrypoint or Coolify change.
- No deployment intelligence is restored.
